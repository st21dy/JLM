<h1>画像投稿フォーム</h1>
<%= form_with model: @article, local:true do |f| %>

  <%= f.file_field :video, :accept => 'video/*,image/jpeg,image/gif,image/png' %>
  
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>説明</h4>
  <%= f.text_area :body %>
  
  <%= f.submit '投稿' %>
  
<% end %>
