<% if current_user.following?(user) %>
  <%= link_to "Unfollow", user_follows_path(user.id), method: :delete %>
<% else %>
  <%= link_to "Follow", user_follows_path(user.id), method: :post %>
<% end %>